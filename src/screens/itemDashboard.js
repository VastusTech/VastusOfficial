

import React, { Component } from 'react';
import {Container, Label, Form, Input, Search, Sidebar, Segment, Button, Menu, Image, Icon, Header, Grid, Message }
    from 'semantic-ui-react';
import CreateItemModal from './createItem';

class ItemDashboard extends Component {
render() {
    return (
    <Grid>
        <Grid.row columns={7}>
            <Grid.Column>
                
            </Grid.Column>
        </Grid.row>
    </Grid>
    );
  }
}
export default ItemDashboard;